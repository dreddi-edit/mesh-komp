---
phase: 34-mesh-folder-improved-auto-generated-files
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified: [mesh-core/src/workspace-operations.js, src/core/infrastructure/state-provision.js]
autonomous: true
requirements: [MESH-01]

must_haves:
  truths:
    - Old buildMeshFileContent function is removed
    - Old provisionMeshFile function is removed
    - Old buildIntelligenceArtifacts function is removed
    - Old provisionIntelligenceArtifacts function is removed
    - Old enqueueIntelligenceJob and intelligence queue code is removed
    - Old provisionDependencyMap function body is removed (or function redirects to provisionMeshFolder)
    - provisionMeshWorkspaceMetadata function body is removed or redirects to provisionMeshFolder
    - No references to .mesh-Intelligence remain in the codebase
    - FRAMEWORK_PATTERNS is defined only once (deduplicated)
  artifacts:
    - mesh-core/src/workspace-operations.js without old generator functions
    - src/core/infrastructure/state-provision.js without old workspace-instructions generator
  key_links:
    - provisionMeshFolder is the sole .mesh generation function
    - No callers reference old function names
---

<objective>
Remove the old .mesh generation code that was replaced by provisionMeshFolder in Plan 01. This includes 5 old functions, the intelligence queue mechanism, and the .mesh-Intelligence subdirectory code. Also remove the duplicate FRAMEWORK_PATTERNS constant.

Purpose: Clean up dead code after the consolidation in Plan 01. The old generators are no longer called.
Output: Leaner workspace-operations.js and state-provision.js with no dead .mesh code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-mesh-folder-improved-auto-generated-files/34-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old generator functions from workspace-operations.js</name>
  <files>mesh-core/src/workspace-operations.js</files>
  <read_first>
    - mesh-core/src/workspace-operations.js (lines 936-1168 — provisionDependencyMap, intelligenceQueue, provisionIntelligenceArtifacts, buildIntelligenceArtifacts; lines 1180-1473 — buildMeshFileContent, provisionMeshFile)
  </read_first>
  <action>
Remove the following functions and their associated code blocks entirely:

1. **`provisionDependencyMap`** (starts around line 936) — generates dependency-map.md. Its useful logic (dependency hub table) is now in `buildFilesMd()`.

2. **`intelligenceQueuePending` array, `intelligenceQueueRunning` flag, `enqueueIntelligenceJob` function, `drainIntelligenceQueue` function** (around lines 984-1004) — the intelligence queue mechanism is replaced by the direct `provisionMeshFolder()` call.

3. **`provisionIntelligenceArtifacts`** (around line 1006) — provisions `.mesh-Intelligence/` which is being killed.

4. **`buildIntelligenceArtifacts`** (around line 1044) — builds the 4 separate intelligence files (api-surface.md, tech-stack.json, style-guide.md, todo-summary.md). All this data is now in the 3 consolidated files.

5. **`buildMeshFileContent`** (around line 1180) — builds the big single `.mesh` intelligence file. Replaced by `buildProjectJson`, `buildFilesMd`, `buildRulesMd`.

6. **`provisionMeshFile`** (around line 1432) — writes the single `.mesh` file. Replaced by `provisionMeshFolder`.

Also remove any `module.exports` entries for these functions if they're exported. Keep `provisionMeshFolder` and its 3 helpers as the only .mesh generation code.

**Deduplicate FRAMEWORK_PATTERNS:** The old code had the same regex array in both `buildIntelligenceArtifacts` and `buildMeshFileContent`. After removing those, verify that `buildProjectJson` (from Plan 01) is the only place with `FRAMEWORK_PATTERNS`. If the constant was duplicated into the new function, extract it to a module-level constant so it's defined once.
  </action>
  <acceptance_criteria>
    - `grep -c "function buildMeshFileContent" mesh-core/src/workspace-operations.js` returns 0
    - `grep -c "function provisionMeshFile" mesh-core/src/workspace-operations.js` returns 0
    - `grep -c "function buildIntelligenceArtifacts" mesh-core/src/workspace-operations.js` returns 0
    - `grep -c "function provisionIntelligenceArtifacts" mesh-core/src/workspace-operations.js` returns 0
    - `grep -c "intelligenceQueuePending\|intelligenceQueueRunning\|enqueueIntelligenceJob\|drainIntelligenceQueue" mesh-core/src/workspace-operations.js` returns 0
    - `grep -c "function provisionDependencyMap" mesh-core/src/workspace-operations.js` returns 0
    - `grep -c "mesh-Intelligence" mesh-core/src/workspace-operations.js` returns 0
    - `grep -c "FRAMEWORK_PATTERNS" mesh-core/src/workspace-operations.js` returns exactly 1 (single definition)
    - `grep "function provisionMeshFolder" mesh-core/src/workspace-operations.js` still returns a match (new code preserved)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "buildMeshFileContent\|buildIntelligenceArtifacts\|provisionMeshFile\b" mesh-core/src/workspace-operations.js | grep -q "^0$" && echo "PASS: old functions removed" || echo "FAIL: old functions still exist"</automated>
  </verify>
  <done>
    - 6 old functions removed from workspace-operations.js
    - Intelligence queue mechanism removed
    - .mesh-Intelligence references removed
    - FRAMEWORK_PATTERNS defined once at module level
    - provisionMeshFolder and its helpers are the sole .mesh generation code
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove old workspace-instructions generator from state-provision.js</name>
  <files>src/core/infrastructure/state-provision.js</files>
  <read_first>
    - src/core/infrastructure/state-provision.js (lines 126-206 — provisionMeshWorkspaceMetadata)
    - mesh-core/src/workspace-operations.js (verify provisionMeshFolder exists and covers workspace-instructions content via rules.md)
  </read_first>
  <action>
Check if `provisionMeshWorkspaceMetadata` is called from anywhere besides its definition. Search for callers:

```bash
grep -rn "provisionMeshWorkspaceMetadata" src/ mesh-core/ --include="*.js"
```

**If called from other files:** Replace those calls with `provisionMeshFolder()` (the new function handles workspace instructions via `rules.md`). Then remove `provisionMeshWorkspaceMetadata` from `state-provision.js`.

**If only defined in state-provision.js and exported but not called elsewhere:** Remove the function entirely. Also remove the `MESH_SYSTEM_PROMPT` constant and any helper functions used only by `provisionMeshWorkspaceMetadata` (like `generateMeshWorkspaceTree`, `generateMeshWorkspaceTreeFromManifest`, `readPackageJsonSummary` if they're only used there — check before removing).

Remove the function from `module.exports` if it's exported.

**Important:** `generateMeshWorkspaceTree` and `readPackageJsonSummary` may be used by the new `provisionMeshFolder` indirectly. Only remove helpers that have zero remaining callers.
  </action>
  <acceptance_criteria>
    - `grep -c "provisionMeshWorkspaceMetadata" src/core/infrastructure/state-provision.js` returns 0 or 1 (only if redirect stub)
    - `grep -rn "provisionMeshWorkspaceMetadata" src/ mesh-core/ --include="*.js" | wc -l` returns 0 (no callers)
    - `grep -c "MESH_SYSTEM_PROMPT" src/core/infrastructure/state-provision.js` returns 0 (removed with the function)
    - `node --test --test-force-exit --test-timeout=120000` — all project tests pass
  </acceptance_criteria>
  <verify>
    <automated>grep -c "provisionMeshWorkspaceMetadata" src/core/infrastructure/state-provision.js | grep -q "^0$" && echo "PASS: old function removed" || echo "FAIL: old function still exists"</automated>
  </verify>
  <done>
    - provisionMeshWorkspaceMetadata removed from state-provision.js
    - MESH_SYSTEM_PROMPT constant removed
    - No callers reference the old function
    - Unused helper functions cleaned up
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `grep -c "buildMeshFileContent\|buildIntelligenceArtifacts\|provisionMeshFile\b" mesh-core/src/workspace-operations.js` → 0
2. `grep -c "mesh-Intelligence" mesh-core/src/workspace-operations.js` → 0
3. `grep -c "provisionMeshWorkspaceMetadata" src/core/infrastructure/state-provision.js` → 0
4. `grep -c "FRAMEWORK_PATTERNS" mesh-core/src/workspace-operations.js` → 1 (single definition)
5. `node --test --test-force-exit --test-timeout=120000` → all tests pass
</verification>

<success_criteria>
- All 6 old generator functions removed from workspace-operations.js
- Intelligence queue mechanism removed
- .mesh-Intelligence directory code eliminated
- provisionMeshWorkspaceMetadata and MESH_SYSTEM_PROMPT removed from state-provision.js
- FRAMEWORK_PATTERNS deduplicated to single definition
- provisionMeshFolder is the sole .mesh generation function
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-mesh-folder-improved-auto-generated-files/34-02-SUMMARY.md`
</output>
