---
phase: 24
plan: "02"
title: "Infrastructure + Context + Runs Splits"
wave: 1
depends_on: []
files_modified:
  - src/core/infrastructure/path-safety.js
  - src/core/infrastructure/metadata.js
  - src/core/infrastructure/s3-ops.js
  - src/core/infrastructure/job-queue.js
  - src/core/infrastructure/index.js
  - src/core/workspace-infrastructure.js
  - src/core/workspace-context.js
  - src/core/assistant-runs.js
autonomous: true
requirements:
  - QUAL-01
---

# Plan 24-02: Infrastructure + Context + Runs Splits

<objective>
Split workspace-infrastructure.js (1,191 lines), workspace-context.js (1,146 lines), and assistant-runs.js (1,130 lines) into focused modules under 400 lines each.
</objective>

<tasks>

<task id="1" name="Split workspace-infrastructure.js">
<read_first>
- src/core/workspace-infrastructure.js
</read_first>
<action>
1. Create directory `src/core/infrastructure/`
2. Extract:
   - `path-safety.js` — toSafePath, ensureWorkspaceOwnedPath, sanitizeTerminalSegment, path validation
   - `metadata.js` — Workspace metadata store operations
   - `s3-ops.js` — S3 blob storage operations
   - `job-queue.js` — Job queue management
   - `index.js` — Re-exports
3. Update `src/core/workspace-infrastructure.js` to re-export facade
</action>
<acceptance_criteria>
- Directory `src/core/infrastructure/` exists with 5 files
- `node -e "require('./src/core/workspace-infrastructure')"` doesn't throw
- Each file under 400 lines
- `npm test` passes
</acceptance_criteria>
</task>

<task id="2" name="Split workspace-context.js">
<read_first>
- src/core/workspace-context.js
</read_first>
<action>
1. Create directory `src/core/context/`
2. Extract by responsibility:
   - `file-cache.js` — File open cache operations
   - `terminal-sessions.js` — Terminal session management
   - `workspace-fallback.js` — Workspace fallback/tunnel logic
   - `index.js` — Re-exports
3. Update `src/core/workspace-context.js` to re-export facade
</action>
<acceptance_criteria>
- Directory `src/core/context/` exists
- `node -e "require('./src/core/workspace-context')"` doesn't throw
- Each file under 400 lines
- `npm test` passes
</acceptance_criteria>
</task>

<task id="3" name="Split assistant-runs.js">
<read_first>
- src/core/assistant-runs.js
</read_first>
<action>
1. Create directory `src/core/assistant/`
2. Extract:
   - `run-lifecycle.js` — Run create, execute, complete, fail
   - `proposals.js` — Proposal generation logic
   - `batch-edit.js` — Batch editing operations
   - `index.js` — Re-exports
3. Update `src/core/assistant-runs.js` to re-export facade
</action>
<acceptance_criteria>
- Directory `src/core/assistant/` exists
- `node -e "require('./src/core/assistant-runs')"` doesn't throw
- Each file under 400 lines
- `npm test` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npm test` passes
- All split files under 400 lines
- Existing require() paths work
</verification>

<must_haves>
- workspace-infrastructure, workspace-context, assistant-runs decomposed
- No file exceeds 400 lines
- Re-export facades maintain compatibility
</must_haves>
