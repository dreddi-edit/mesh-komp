---
phase: 24
plan: "03"
title: "Mesh-Core Splits + Deduplication + Index Reduction"
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - mesh-core/src/compression/
  - mesh-core/src/workspace/
  - src/core/index.js
  - src/core/infrastructure/path-safety.js
  - src/core/auth.js
autonomous: true
requirements:
  - QUAL-01
  - QUAL-03
---

# Plan 24-03: Mesh-Core Splits + Deduplication + Index Reduction

<objective>
Split mesh-core monoliths (compression-core.cjs 2,568 lines, workspace-operations.js 2,326 lines), deduplicate shared functions, and reduce src/core/index.js to a minimal re-export facade.
</objective>

<tasks>

<task id="1" name="Split compression-core.cjs">
<read_first>
- mesh-core/src/compression-core.cjs
</read_first>
<action>
1. Create directory `mesh-core/src/compression/`
2. Extract logical sections (marked by `// ── Section ──` headers):
   - `brotli.js` — Brotli compression/decompression
   - `capsule.js` — Capsule format encoding/decoding
   - `transport.js` — Transport layer compression
   - `pipeline.js` — Compression pipeline orchestration
   - `index.cjs` — Re-exports
3. Update `mesh-core/src/compression-core.cjs` to re-export facade
</action>
<acceptance_criteria>
- Directory `mesh-core/src/compression/` exists
- `node -e "require('./mesh-core/src/compression-core.cjs')"` doesn't throw
- Each file under 400 lines
</acceptance_criteria>
</task>

<task id="2" name="Split workspace-operations.js">
<read_first>
- mesh-core/src/workspace-operations.js
</read_first>
<action>
1. Create directory `mesh-core/src/workspace/`
2. Extract:
   - `file-ops.js` — File operations
   - `sync-ops.js` — Sync operations
   - `helpers.js` — Shared helpers
   - `index.js` — Re-exports
3. Update `mesh-core/src/workspace-operations.js` to re-export facade
</action>
<acceptance_criteria>
- Directory `mesh-core/src/workspace/` exists
- `node -e "require('./mesh-core/src/workspace-operations')"` doesn't throw
- Each file under 400 lines
</acceptance_criteria>
</task>

<task id="3" name="Deduplicate shared functions">
<read_first>
- src/core/infrastructure/path-safety.js
- src/core/model-providers.js
- src/core/auth.js
- secure-db.js
- assistant-core.js
</read_first>
<action>
1. `toSafePath()` — keep in `src/core/infrastructure/path-safety.js`, remove duplicate from model-providers.js. Add `const { toSafePath } = require('./infrastructure/path-safety');` where needed.

2. `normalizeEmail()` — keep in `src/core/auth.js`, remove duplicate from `secure-db.js`. Add `const { normalizeEmail } = require('./core/auth');` in secure-db.js.

3. Path scoring — consolidate into `src/core/infrastructure/path-safety.js` if duplicated between workspace-ops and assistant-core.js.

Each deduplication: keep the canonical copy, replace others with imports.
</action>
<acceptance_criteria>
- `grep -r "function toSafePath" src/` returns exactly 1 match
- `grep -r "function normalizeEmail" src/ secure-db.js` returns exactly 1 match
- `npm test` passes
</acceptance_criteria>
</task>

<task id="4" name="Reduce src/core/index.js">
<read_first>
- src/core/index.js
</read_first>
<action>
Reduce `src/core/index.js` (1,200 lines) to a minimal re-export facade:

1. Remove all domain logic that was extracted to sub-modules in plans 24-01 and 24-02
2. Keep only: imports of sub-modules + `module.exports` object that re-exports everything
3. Target: under 200 lines (just wiring)
4. Any global state initialization that remains should be clearly marked

Pattern:
```javascript
'use strict';
const providers = require('./providers');
const workspace = require('./workspace');
const infrastructure = require('./infrastructure');
const context = require('./context');
const assistant = require('./assistant');
const auth = require('./auth');

module.exports = {
  ...providers,
  ...workspace,
  ...infrastructure,
  ...context,
  ...assistant,
  ...auth,
};
```
</action>
<acceptance_criteria>
- `wc -l src/core/index.js` shows under 200 lines
- `node -e "const core = require('./src/core'); console.log(typeof core.streamAnthropicNative)"` prints `function`
- `npm test` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npm test` passes
- All monolith files under 400 lines
- Duplicated functions exist in exactly one location
- core/index.js is a slim facade
</verification>

<must_haves>
- compression-core.cjs and workspace-operations.js decomposed
- toSafePath, normalizeEmail exist in exactly one location
- src/core/index.js under 200 lines
</must_haves>
