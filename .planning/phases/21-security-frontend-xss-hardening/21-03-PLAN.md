---
phase: 21
plan: "03"
title: "Remaining Files + Visual Verification"
wave: 3
depends_on: ["21-02"]
files_modified:
  - assets/settings.js
  - assets/features/*.js
autonomous: false
requirements:
  - SEC-05
---

# Plan 21-03: Remaining Files + Visual Verification

<objective>
Harden remaining frontend files (settings, features), remove fragile `esc()` helper where replaced by DOMPurify, and visually verify all 16 pages have no regressions.
</objective>

<tasks>

<task id="1" name="Harden settings.js and feature scripts">
<read_first>
- assets/settings.js
- assets/features/
</read_first>
<action>
1. In `assets/settings.js` (1,276 lines):
   - Settings page builds UI dynamically — find innerHTML with user-provided settings values
   - API key displays, provider names, model names — use `textContent`
   - Settings form labels from config — static, low risk, leave as-is unless user-editable

2. In `assets/features/*.js`:
   - Each feature IIFE — check for innerHTML with user-derived content
   - Focus on search results display, terminal output, file previews
   - Replace with safe DOM APIs

3. Remove or deprecate the fragile `esc()` helper wherever it's been replaced by DOMPurify or DOM APIs. If `esc()` is still used for some LOW RISK sites, leave it with a comment noting it's only for static content.
</action>
<acceptance_criteria>
- Settings page renders user values via textContent where appropriate
- Feature scripts use safe DOM APIs for user content
- `npm test` passes
</acceptance_criteria>
</task>

<task id="2" name="Visual verification of all pages" autonomous="false">
<read_first>
- views/
</read_first>
<action>
Human checkpoint: Start dev server and verify each of the 16 pages:

1. index.html — landing page renders correctly
2. app.html — main workspace view, chat, file tree, graph all functional
3. terminal.html — terminal launches, output renders
4. statistics.html — stats charts and data display
5. docs.html — documentation pages render
6. settings pages (6) — all settings display and save correctly
7. login.html / register.html — forms work
8. voice pages — UI loads
9. error pages — display correctly

For each page verify:
- No visual regressions from XSS hardening
- Interactive features work (click, type, submit)
- Browser console has no new JS errors
</action>
<acceptance_criteria>
- All 16 pages load without JS errors
- Chat messages with markdown formatting render correctly
- File tree with special character filenames displays safely
- Graph labels display file paths as text
- No visual regressions detected
</acceptance_criteria>
</task>

</tasks>

<verification>
- All frontend JS files have user-content innerHTML replaced
- 16 pages verified visually with no regressions
- Browser console clean of new errors
</verification>

<must_haves>
- All user-content innerHTML replaced across all frontend files
- No visual regressions across all 16 pages
- esc() helper deprecated or removed where DOMPurify is used
</must_haves>
