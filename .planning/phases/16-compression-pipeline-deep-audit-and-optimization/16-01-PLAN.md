---
phase: 16
plan: "01"
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-AUDIT.md"
autonomous: true
title: "Compression Pipeline — Deep Code Audit"
objective: |
  Read every compression-related source file in depth and produce a structured AUDIT.md
  documenting: the exact data flow with function names + line numbers, token cost per mode,
  caching gaps, and a prioritized list of improvements to implement in Plan 02.
  This plan is READ-ONLY — no production source files are modified.
must_haves:
  - "16-AUDIT.md exists at .planning/phases/16-compression-pipeline-deep-audit-and-optimization/"
  - "AUDIT.md contains ## Data Flow section with function names and line numbers"
  - "AUDIT.md contains ## Token Cost Analysis section with per-mode estimates"
  - "AUDIT.md contains ## Findings section with at least 5 numbered findings"
  - "AUDIT.md contains ## Implementation Order section ranking improvements by impact"
---

# Plan 16-01: Compression Pipeline Deep Code Audit

## Overview

Read all compression-related source files end-to-end and produce a structured AUDIT.md.
The purpose is to give the executor (Opus 4.7) a deep, first-hand understanding of the
pipeline before any code changes are made in Plan 02.

---

## Tasks

<task id="16-01-01" title="Read compression engine core">
<read_first>
- mesh-core/src/compression-core.cjs (read in 3 passes: lines 1-200, 200-500, 500-900)
- mesh-core/src/compression-utils.cjs (full file)
- mesh-core/src/tree-sitter-worker.cjs (full file)
</read_first>

<action>
Read the three core files listed above. Document in your working notes:
1. All exported functions from compression-core.cjs — name, signature, purpose
2. The buildCodeCapsule() decision tree (tree-sitter vs heuristic vs plain text)
3. The worker pool implementation — how tasks are dispatched, how errors are handled
4. All size/limit constants and their env var overrides
5. Transport encoding strategy (zstd vs brotli, chunk sizes, parallelism)
6. How buildWorkspaceFileRecord() wraps buildCodeCapsule() for storage
These notes feed directly into the AUDIT.md created in task 16-01-05.
</action>

<acceptance_criteria>
- Task produces working notes (held in executor memory for 16-01-05)
- No source files are modified
</acceptance_criteria>
</task>

<task id="16-01-02" title="Read indexability gate and storage layer">
<read_first>
- src/core/workspace-infrastructure.js lines 70-115 (isWorkspaceIndexablePath, skip constants)
- src/core/workspace-infrastructure.js lines 280-340 (generateMeshWorkspaceTree)
- src/core/workspace-infrastructure.js lines 495-520 (scanLocalWorkspaceFiles)
- src/core/index.js lines 295-310 (LOCAL_WORKSPACE_SKIP_EXTENSIONS definition)
</read_first>

<action>
Read the sections listed above. Document:
1. Exact regex for LOCAL_WORKSPACE_SKIP_EXTENSIONS (src/core/index.js:301) — does it include .min.js?
2. All call sites of isWorkspaceIndexablePath() — where the indexability gate is enforced
3. Where LOCAL_WORKSPACE_SKIP_EXTENSIONS is imported vs. where the regex is defined
4. MAX_FILE_CHARS limit and where it's enforced
</action>

<acceptance_criteria>
- No source files are modified
- Executor has documented whether .min.js/.min.css are currently excluded
</acceptance_criteria>
</task>

<task id="16-01-03" title="Read budget resolution and capsule loading">
<read_first>
- src/core/workspace-ops.js lines 1420-1500 (resolveAdaptiveCompressedContextBudget)
- src/core/workspace-ops.js lines 1580-1640 (inferReferencedFilesFromWorkspace + LRU cache)
- src/core/workspace-context.js lines 780-895 (buildCapsuleContextBlock, loadCapsuleContextEntries)
- src/core/workspace-context.js lines 895-960 (loadRecoveredSpanEntries)
</read_first>

<action>
Read the sections listed. Document:
1. All 4 budget modes (single-file, active-file, balanced, broad) — exact char limits per mode
2. How inferReferencedFilesFromWorkspace ranks files — what scoring function is used
3. How loadCapsuleContextEntries enforces per-file char limits — truncation path
4. The exact truncation marker string at workspace-context.js ~line 859
5. Whether entries are sorted by relevance or alphabetically after loading
</action>

<acceptance_criteria>
- No source files are modified
- Executor has documented exact truncation marker string for task 16-02-03
</acceptance_criteria>
</task>

<task id="16-01-04" title="Read chat routes and codec injection">
<read_first>
- src/routes/assistant-chat.routes.js (full file)
- src/core/model-providers.js (search for: encodeMeshModelCodec, buildMeshCodecContextDocument, meshCodecSessionState, injectMeshSystemPrompt — read surrounding ~50 lines each)
</read_first>

<action>
Read the files. Document:
1. The exact sequence of function calls in POST /api/assistant/chat (lines 66-262)
2. The exact sequence of function calls in POST /api/assistant/chat/stream (lines 291-440)
3. Which code blocks are identical between the two routes — line ranges
4. How buildMeshCodecContextDocument() content is structured — what tokens/size it produces
5. How injectMeshSystemPrompt() places the codec context — system message vs user message
6. The codec session state key structure — how per-session caching works
</action>

<acceptance_criteria>
- No source files are modified
- Executor has identified the exact duplicate code block line ranges between /chat and /chat/stream
</acceptance_criteria>
</task>

<task id="16-01-05" title="Write AUDIT.md with findings and implementation plan">
<read_first>
- .planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-RESEARCH.md
- Notes from tasks 16-01-01 through 16-01-04 (held in executor memory)
</read_first>

<action>
Create .planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-AUDIT.md with:

## Data Flow
Complete data flow diagram with actual function names and line numbers for each step:
file path → isWorkspaceIndexablePath() (infra.js:81) → ... → LLM API call

## Token Cost Analysis
Per-mode breakdown table:
| Mode | Max Files | First File Chars | Budget Chars Total | Approx Input Tokens |
For each mode: single-file, active-file, balanced, broad

## Findings
Numbered list of at least 5 findings with:
- Finding #: [title]
- Severity: CRITICAL / HIGH / MEDIUM / LOW
- Location: file:line
- Description: what the problem is
- Evidence: what you read that confirms it

## Caching Gaps
List any identified caching opportunities not currently implemented

## Implementation Order
Ranked list of improvements from Plan 02 by estimated impact:
1. [most impactful] — [estimated token savings or performance gain]
2. ...

## Verification Notes
Any discrepancies between RESEARCH.md findings and what was actually found in the code
</action>

<acceptance_criteria>
- .planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-AUDIT.md exists
- grep "## Data Flow" .planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-AUDIT.md exits 0
- grep "## Token Cost Analysis" .planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-AUDIT.md exits 0
- grep "## Findings" .planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-AUDIT.md exits 0
- grep "## Implementation Order" .planning/phases/16-compression-pipeline-deep-audit-and-optimization/16-AUDIT.md exits 0
- File has at least 100 lines
</acceptance_criteria>
</task>

---

## Verification Criteria

- 16-AUDIT.md created with all required sections
- No production source files modified
- npm test passes (run after plan to confirm no accidental changes): `npm test`
