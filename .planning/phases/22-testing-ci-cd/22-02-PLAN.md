---
phase: 22
plan: "02"
title: "Core Module Unit Tests"
wave: 2
depends_on: ["22-01"]
files_modified:
  - test/workspace-ops.test.js
  - test/workspace-infrastructure.test.js
  - test/workspace-context.test.js
  - test/assistant-runs.test.js
  - test/voice-agent.test.js
  - test/deployments.test.js
autonomous: true
requirements:
  - TEST-01
---

# Plan 22-02: Core Module Unit Tests

<objective>
Write dedicated test files for 6 untested core modules targeting >60% line coverage each. Uses manual stubs and in-memory fakes consistent with existing test patterns.
</objective>

<tasks>

<task id="1" name="Expand workspace-ops tests">
<read_first>
- test/workspace-ops.test.js
- src/core/workspace-ops.js
</read_first>
<action>
Expand existing `test/workspace-ops.test.js` (currently 84 lines) to cover:

1. File read/write operations with mocked filesystem
2. Search/grep functionality
3. Batch operations
4. Error cases (file not found, permission denied)
5. Path safety validation

Use `node:test` describe/it blocks. Create stubs for filesystem and S3 dependencies.
Target: >60% line coverage of `src/core/workspace-ops.js` (1,723 lines).
</action>
<acceptance_criteria>
- `test/workspace-ops.test.js` exceeds 200 lines
- `npm test` passes including new tests
- `c8 report` shows >60% line coverage for workspace-ops.js
</acceptance_criteria>
</task>

<task id="2" name="Create workspace-infrastructure tests">
<read_first>
- src/core/workspace-infrastructure.js
</read_first>
<action>
Create `test/workspace-infrastructure.test.js` covering:

1. Path safety functions (toSafePath, ensureWorkspaceOwnedPath)
2. Metadata store operations with in-memory stub
3. Git wrapper functions
4. S3 blob operations with mocked S3 client
5. Job queue operations

Target: >60% line coverage of `src/core/workspace-infrastructure.js` (1,191 lines).
</action>
<acceptance_criteria>
- File `test/workspace-infrastructure.test.js` exists
- `npm test` passes
- `c8 report` shows >60% line coverage for workspace-infrastructure.js
</acceptance_criteria>
</task>

<task id="3" name="Create workspace-context tests">
<read_first>
- src/core/workspace-context.js
</read_first>
<action>
Create `test/workspace-context.test.js` covering:

1. File open cache operations
2. Workspace fallback logic
3. Terminal session management
4. Context building for AI requests

Target: >60% line coverage of `src/core/workspace-context.js` (1,146 lines).
</action>
<acceptance_criteria>
- File `test/workspace-context.test.js` exists
- `npm test` passes
</acceptance_criteria>
</task>

<task id="4" name="Create assistant-runs tests">
<read_first>
- src/core/assistant-runs.js
</read_first>
<action>
Create `test/assistant-runs.test.js` covering:

1. Run lifecycle (create, execute, complete, fail)
2. Proposal generation logic
3. Batch editing operations
4. Run state management

Target: >60% line coverage of `src/core/assistant-runs.js` (1,130 lines).
</action>
<acceptance_criteria>
- File `test/assistant-runs.test.js` exists
- `npm test` passes
</acceptance_criteria>
</task>

<task id="5" name="Create voice-agent and deployments tests">
<read_first>
- src/core/voice-agent.js
- src/core/deployments.js
</read_first>
<action>
1. Create `test/voice-agent.test.js` covering:
   - Voice chat agent initialization
   - Tool use (workspace read/write/search)
   - Audio processing stubs
   Target: >60% line coverage of voice-agent.js (851 lines)

2. Create `test/deployments.test.js` covering:
   - Deployment queue operations
   - Policy management
   - Error handling
   Target: >60% line coverage of deployments.js (210 lines)
</action>
<acceptance_criteria>
- Files `test/voice-agent.test.js` and `test/deployments.test.js` exist
- `npm test` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npm test` passes with all new and existing tests
- `npm run test:coverage` shows >60% line coverage for all 6 modules
</verification>

<must_haves>
- Dedicated test files for all 6 untested modules
- Each module >60% line coverage
- All existing tests still pass
</must_haves>
