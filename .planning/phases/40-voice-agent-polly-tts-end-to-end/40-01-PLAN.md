---
phase: "40"
plan: "40-01"
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/core/voice-agent.js
requirements_addressed:
  - VOIC-03
---

# Plan 40-01: Remove sendAzureEvent Dead Code + Verify Polly Path

**Objective:** Remove the 4 `sendAzureEvent(...)` call sites in `src/core/voice-agent.js` that are no-ops (the caller always passes `sendAzureEvent: () => {}`). Retain the parameter acceptance at line 259 for interface stability. Then statically verify the Polly TTS path is complete and no Azure SDK references exist in the voice audio path.

**Wave:** 1 (autonomous — static code changes + grep verification)

---

## Task 40-01-01: Remove sendAzureEvent call blocks from resolveRunAction

<task id="40-01-01">
<title>Remove sendAzureEvent calls from resolveRunAction function</title>

<read_first>
- src/core/voice-agent.js (lines 530–560 — see the two sendAzureEvent calls following the voice.narration sendClientEvent in resolveRunAction)
</read_first>

<action>
In `src/core/voice-agent.js`, inside the `resolveRunAction` function, remove the following two call blocks entirely (they appear after the `sendClientEvent({ type: 'voice.narration', ... })` block):

Remove block 1 (~lines 537–551):
```js
sendAzureEvent({
  type: 'conversation.item.create',
  item: {
    type: 'message',
    role: 'user',
    content: [
      {
        type: 'input_text',
        text: decision === 'approve'
          ? `The operator approved action ${actionId} in run ${runId}. Continue.`
          : `The operator rejected action ${actionId} in run ${runId}. Adjust and continue.`,
      },
    ],
  },
});
```

Remove block 2 (~line 552):
```js
sendAzureEvent({ type: 'response.create' });
```

Do NOT touch the `sendClientEvent(...)` calls, `rememberToolResult(...)`, or the `return { ok: true, ... }` statement.
</action>

<acceptance_criteria>
- `grep -n "sendAzureEvent" src/core/voice-agent.js` → matches only lines 259, 794, 802 (the parameter assignment and the tool-output block — not the resolveRunAction block)
- `grep -c "sendAzureEvent" src/core/voice-agent.js` → 3
- `grep "conversation.item.create" src/core/voice-agent.js` → 1 remaining match (the tool output block, not the action resolution block)
- `node --check src/core/voice-agent.js` exits 0 (syntax valid)
</acceptance_criteria>
</task>

---

## Task 40-01-02: Remove sendAzureEvent calls from tool execution handler

<task id="40-01-02">
<title>Remove sendAzureEvent calls from tool call output handler</title>

<read_first>
- src/core/voice-agent.js (lines 785–815 — see the two sendAzureEvent calls following the `executeTool` try/catch block)
</read_first>

<action>
In `src/core/voice-agent.js`, inside the tool execution handler (the block that calls `executeTool` and builds `output = JSON.stringify(result)`), remove the following two call blocks entirely (they appear after `const output = JSON.stringify(result)`):

Remove block 1 (~lines 794–801):
```js
sendAzureEvent({
  type: 'conversation.item.create',
  item: {
    type: 'function_call_output',
    call_id: callId,
    output,
  },
});
```

Remove block 2 (~line 802):
```js
sendAzureEvent({ type: 'response.create' });
```

Do NOT touch the `executeTool(...)` call, the `try/catch` block, `const output = JSON.stringify(result)`, or the `return result` statement.
</action>

<acceptance_criteria>
- `grep -n "sendAzureEvent" src/core/voice-agent.js` → matches only line 259 (the parameter assignment only)
- `grep -c "sendAzureEvent" src/core/voice-agent.js` → 1
- `grep "function_call_output" src/core/voice-agent.js` → 0 matches
- `node --check src/core/voice-agent.js` exits 0 (syntax valid)
</acceptance_criteria>
</task>

---

## Task 40-01-03: Static verification of Polly TTS path

<task id="40-01-03">
<title>Verify no Azure SDK imports in voice audio path and Polly path is wired</title>

<read_first>
- src/core/voice-aws-audio.js (verify AWS-only imports, synthesizeSpeech function)
- src/routes/realtime.routes.js (verify synthesizeSpeech call in streamSpeechResponse, verify sendAzureEvent is no-op)
- src/core/voice-agent.js (verify only parameter no-op remains)
</read_first>

<action>
Run the following verification commands and confirm each produces the expected output:

1. `grep -n "require.*azure\|require.*openai-realtime\|@azure" src/core/voice-aws-audio.js src/routes/realtime.routes.js src/core/voice-agent.js` → 0 matches (no Azure SDK imports in voice audio path)

2. `grep -n "synthesizeSpeech" src/routes/realtime.routes.js` → at least 1 match (Polly TTS is called)

3. `grep -n "sendAzureEvent" src/core/voice-agent.js` → exactly 1 match (the parameter assignment at line 259 only)

4. `grep -n "MESH_VOICE_POLLY_VOICE\|Joanna" src/core/voice-aws-audio.js` → at least 1 match (env var and default voice present)

5. `node --check src/core/voice-agent.js` exits 0

If any command produces unexpected output, that is a blocker — do not proceed to commit.
</action>

<acceptance_criteria>
- `grep -c "require.*azure\|@azure" src/core/voice-aws-audio.js src/routes/realtime.routes.js src/core/voice-agent.js` → 0 total
- `grep -c "synthesizeSpeech" src/routes/realtime.routes.js` → ≥ 1
- `grep -c "sendAzureEvent" src/core/voice-agent.js` → 1
- `grep -c "MESH_VOICE_POLLY_VOICE" src/core/voice-aws-audio.js` → ≥ 1
- `node --check src/core/voice-agent.js` exits 0
</acceptance_criteria>
</task>

---

## Verification

### Must-Haves (from Phase Goal)

| # | Criterion | How to verify |
|---|-----------|--------------|
| 1 | No Azure SDK calls in the audio path | `grep -r "require.*azure\|@azure" src/core/voice-aws-audio.js src/routes/realtime.routes.js src/core/voice-agent.js` → 0 matches |
| 2 | Polly TTS is called in streamSpeechResponse | `grep "synthesizeSpeech" src/routes/realtime.routes.js` → ≥ 1 match |
| 3 | sendAzureEvent call sites removed (interface kept) | `grep -c "sendAzureEvent" src/core/voice-agent.js` → 1 (parameter only) |
| 4 | MESH_VOICE_POLLY_VOICE env var supported, defaults to Joanna | `grep "MESH_VOICE_POLLY_VOICE\|Joanna" src/core/voice-aws-audio.js` → ≥ 1 match |
| 5 | No syntax errors introduced | `node --check src/core/voice-agent.js` → exit 0 |

### No-Regression Check

- `grep -c "sendAzureEvent" src/routes/realtime.routes.js` → 1 (the no-op assignment comment line — unchanged)
- `grep -c "createVoiceAgentSession" src/routes/realtime.routes.js` → ≥ 1 (voice session creation unchanged)
