# Security Audit Summary — 260416-43t

**Date:** 2026-04-16
**Commit:** dcf8a80

## Fixed (CRITICAL / HIGH)

| # | Severity | Issue | File | Fix |
|---|----------|-------|------|-----|
| 1 | 🔴 CRITICAL | Default demo password `12345` — trivially guessable credential used as fallback when `MESH_DEMO_USER_PASSWORD` is unset | `src/config/index.js:131` | Empty string fallback — demo login disabled when env var is not set |
| 2 | 🟠 HIGH | Timing oracle: demo password compared with `===` (string equality leaks timing info) | `src/routes/auth.routes.js:110` | `crypto.timingSafeEqual()` with length check |
| 3 | 🟠 HIGH | `X-Forwarded-For` blindly trusted — any client can set this header to spoof their IP and bypass rate limiting | `src/middleware/rate-limiter.js:12` | Trust header only when direct connection comes from RFC-1918/loopback range (proxy/ALB) |
| 4 | 🟠 HIGH | Terminal WebSocket: `?shell=` query param passed unsanitized to `node-pty.spawn()` — attacker could spawn arbitrary server binaries | `src/routes/terminal.routes.js:251` | Allowlist: `{bash, sh, zsh, fish, /bin/bash, /bin/sh, /bin/zsh, /usr/bin/fish}` |
| 5 | 🟠 HIGH | `/api/chat`: model ID forwarded to Anthropic API without validation — could trigger unexpected model routing or cost escalation | `src/routes/app.routes.js:537` | `ALLOWED_CHAT_MODELS` Set with 5 permitted model IDs; unknown model falls back to `claude-opus-4-6` |

## Fixed (CVE)

| Package | Severity | Advisory | Fix |
|---------|----------|----------|-----|
| `follow-redirects ≤1.15.11` | Moderate | GHSA-r4q5-vmmm-2653 — credential header leak on cross-domain redirect | `npm audit fix` → 0 vulnerabilities |

## Documented (MEDIUM / LOW)

| # | Issue | File | Risk | Recommendation |
|---|-------|------|------|----------------|
| 1 | 🟡 MEDIUM | CSP `unsafe-inline` on `script-src` weakens XSS mitigation | `src/server.js:39` | Move inline scripts to external files + use nonce-based CSP; not fixed now as it requires frontend changes |
| 2 | 🔵 LOW | `sanitizeContent` in app-routes only strips `sk-...` keys — doesn't catch AWS secret keys, Bearer tokens, etc. in rendered doc content | `src/routes/app.routes.js:151` | Extend pattern to cover `AKIA`, `Bearer `, `ghp_` etc. |
| 3 | 🔵 LOW | No explicit `maxSessions` limit on terminal WebSocket connections per user (voice has one, terminal does not) | `src/routes/terminal.routes.js` | Consider adding a `MAX_TERMINAL_SESSIONS_PER_USER` guard similar to `MAX_VOICE_SESSIONS_PER_USER` |

## Areas Audited

- [x] Authentication & session handling
- [x] Input validation (routes, shell params, model IDs)
- [x] Injection risks (terminal shell, command injection surface)
- [x] Secrets / credential handling (demo password, env vars, log sanitization)
- [x] CORS & security headers
- [x] Rate limiting (IP extraction)
- [x] Error information leakage (safeRouteError wrapper)
- [x] Dependency CVEs (npm audit)
- [x] WebSocket auth (terminal + voice — both enforce auth on upgrade)
- [x] Path traversal (docs/file endpoint — protected with `absPath.startsWith(resolvedRoot)`)
